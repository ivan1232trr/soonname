{
  "name": "eventgo",
  "version": "0.0.1",
  "description": "Event GO monorepo root — tooling and workspace coordination only",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev:api": "pnpm --filter @citypulse/api dev",
    "dev:web": "pnpm --filter @citypulse/web dev",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
