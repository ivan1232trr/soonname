{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "TypeScript config for the shared types package; extends base and compiles to dist/",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
